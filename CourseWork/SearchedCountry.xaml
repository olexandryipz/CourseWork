<UserControl x:Class="CourseWork.SearchedCountry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CourseWork"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid Background="Transparent">
        <Button Click="Hello">
            <TextBlock TextAlignment="Center" FontSize="15" x:Name="Denis" TextWrapping="Wrap" Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="296" HorizontalAlignment="Center" Height="47" RenderTransformOrigin="0.5,0.5">
            </TextBlock>
        </Button>
    </Grid>
</UserControl>